# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Env
.env
.env.local
.env.*.local

# Capacitor / Android
# NOT: android/ klasörü BİLEREK commit ediliyor (gitignore'da DEĞİL) — AndroidManifest.xml'e
# eklenen kamera izni gibi native özelleştirmeler kaybolmasın. Sadece Gradle'ın oluşturduğu
# build çıktıları hariç tutuluyor.
android/app/build/
android/build/
android/.gradle/
android/local.properties
android/*.iml
android/.idea/
# `npx cap sync android` her seferinde dist/'ten yeniden kopyalar, commit etmeye gerek yok:
android/app/src/main/assets/public/
android/app/src/main/assets/capacitor.config.json
ios/

# Editor
.vscode/*
!.vscode/extensions.json
.idea/

# Logs
*.log
npm-debug.log*

# TypeScript incremental build cache
*.tsbuildinfo

# OS
.DS_Store

# Keystore (teslimat paketine ayrı ve güvenli şekilde eklenecek, repoya asla girmeyecek)
*.keystore
*.jks
keystore.properties

# Mobil teslimat paketi (AAB/APK/keystore) - repoya değil, ayrı ve güvenli kanaldan iletilecek
teslimat-mobil/
